# Copy this file to .env.local and fill in your credentials for local testing.

# Vercel KV or Upstash Redis: if using Vercel KV, the handler will auto-use it at runtime.
# If using Upstash as a fallback, set these:

# UPSTASH_REDIS_REST_URL=https://us1-XXXX.upstash.io
# UPSTASH_REDIS_REST_TOKEN=XXXX_your_token_here

# For local testing with `vercel dev`:
# Run: vercel env pull .env.local
# Then: vercel dev
