{
  "version": 2,
  "buildCommand": "echo 'No build required'",
  "outputDirectory": ".",
  "routes": [
    { "src": "^/api/(.*)", "dest": "/api/$1" },
    { "src": "^/(?!api)(.*)$", "dest": "/index.html" }
  ]
}
